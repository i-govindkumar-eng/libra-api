apiVersion: v1
kind: Service
metadata:
  name: libra-api
  namespace: libra
spec:
  type: NodePort
  selector:
    app: libra-api
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080   # Access via http://<node-ip>:30080
